# Tongfengguan.github.io

这是使用 Vite + Vue 3 重构的个人主页仓库。

本分支：`feat/vue` 包含项目骨架、路由、ESLint/Prettier、Vitest 以及 GitHub Actions（CI + 部署到 `gh-pages`）。

要本地运行：

```bash
npm install
npm run dev
```

构建并预览：

```bash
npm run build
npm run preview
```
